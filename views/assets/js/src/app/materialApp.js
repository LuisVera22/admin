const materialModel = new MaterialModel();
const materialController = new MaterialController(materialModel, null);
const materialView = new MaterialView(materialController);
materialController.view = materialView;
materialController.view = materialView;